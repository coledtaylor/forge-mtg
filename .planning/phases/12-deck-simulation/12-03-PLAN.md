---
phase: 12-deck-simulation
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - forge-gui-web/frontend/src/components/simulation/SimulationPanel.tsx
  - forge-gui-web/frontend/src/components/simulation/SimulationConfig.tsx
  - forge-gui-web/frontend/src/components/simulation/SimulationProgress.tsx
  - forge-gui-web/frontend/src/components/deck-editor/DeckEditor.tsx
  - forge-gui-web/frontend/src/components/deck-editor/DeckPanel.tsx
autonomous: true
requirements: [SIM-01, SIM-02, SIM-12]

must_haves:
  truths:
    - "User can click Simulate button in deck editor to open simulation panel"
    - "User can select game count (10, 50, 100, 500) before starting"
    - "User can optionally configure which opponent decks to test against"
    - "User can see progress bar updating in real time as games complete"
    - "User can see running win rate and Elo updating as games finish"
    - "User can cancel a running simulation and keep partial results"
  artifacts:
    - path: "forge-gui-web/frontend/src/components/simulation/SimulationPanel.tsx"
      provides: "Main simulation container with config, progress, and results"
      contains: "useSimulation"
    - path: "forge-gui-web/frontend/src/components/simulation/SimulationConfig.tsx"
      provides: "Game count selector and gauntlet configuration"
      contains: "gameCount"
    - path: "forge-gui-web/frontend/src/components/simulation/SimulationProgress.tsx"
      provides: "Live progress bar and running stats"
      contains: "gamesCompleted"
  key_links:
    - from: "DeckEditor.tsx"
      to: "SimulationPanel.tsx"
      via: "Simulate button toggles simulation panel visibility"
      pattern: "SimulationPanel"
    - from: "SimulationPanel.tsx"
      to: "useSimulation.ts"
      via: "Hook provides startSim, cancelSim, progress"
      pattern: "useSimulation"
---

<objective>
Build the simulation trigger UI (Simulate button in DeckEditor), configuration panel (game count, gauntlet selection), and live progress display (progress bar, running stats, cancel button).

Purpose: This is the user's entry point to the simulation feature -- they configure, start, watch progress, and cancel from here.
Output: Working simulation launch flow with real-time progress updates.
</objective>

<execution_context>
@C:/Users/ColeT/Code/java-2D/forge-mtg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ColeT/Code/java-2D/forge-mtg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-deck-simulation/12-CONTEXT.md
@.planning/phases/12-deck-simulation/12-02-SUMMARY.md

<interfaces>
From useSimulation.ts (Plan 02):
- useSimulation(deckName: string) returns { startSim, cancelSim, progress, isRunning, history, refreshHistory }
- startSim(config: SimulationConfig): starts simulation and opens SSE
- cancelSim(): cancels running simulation
- progress: SimulationProgress | null

From simulation-types.ts (Plan 02):
- SimulationConfig { deckName, gameCount: 10|50|100|500, opponentDeckNames?: string[] }
- SimulationProgress { status, gamesCompleted, gamesTotal, winRate, eloRating, matchups, ... }
- SimulationHistoryEntry { id, timestamp, gamesCompleted, gamesTotal, winRate, eloRating }

From elo.ts (Plan 02):
- eloTier(elo: number): string

From DeckEditor.tsx:
- Props: { deckName, format, onBack, onPlayDeck? }
- Has DeckPanel with tabs for deck list, stats

From DeckPanel.tsx:
- Has action buttons row (Back, Play, Import, Export)
- Tabs: Deck, Stats

From useDecks.ts:
- useDecks() returns { decks } -- list of all decks for gauntlet opponent selection
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SimulationConfig and SimulationProgress components</name>
  <files>
    forge-gui-web/frontend/src/components/simulation/SimulationConfig.tsx,
    forge-gui-web/frontend/src/components/simulation/SimulationProgress.tsx
  </files>
  <read_first>
    forge-gui-web/frontend/src/lib/simulation-types.ts,
    forge-gui-web/frontend/src/hooks/useSimulation.ts,
    forge-gui-web/frontend/src/lib/elo.ts,
    forge-gui-web/frontend/src/hooks/useDecks.ts,
    forge-gui-web/frontend/src/components/ui/button.tsx,
    forge-gui-web/frontend/src/components/ui/select.tsx
  </read_first>
  <action>
    1. **SimulationConfig.tsx** -- Configuration panel before simulation starts:
       - Props: { deckName: string, format: string, onStart: (config: SimulationConfig) => void }
       - Game count selector: radio group or segmented control with options [10, 50, 100, 500]
       - Default: 50 games
       - "Configure Gauntlet" expandable section (collapsed by default):
         - When expanded, shows list of available decks (from useDecks hook, filtered to same format)
         - Checkboxes to select/deselect opponents
         - "Select All" / "Deselect All" buttons
         - If collapsed (default), all same-format decks used
       - "Start Simulation" button (primary, prominent) -- calls onStart with config
       - Display: "Testing against {N} opponent decks" summary text
       - Tailwind styling consistent with existing deck editor panels

    2. **SimulationProgress.tsx** -- Live progress display during simulation:
       - Props: { progress: SimulationProgress, onCancel: () => void }
       - Progress bar: "42/100 games complete" with percentage fill bar
       - Running stats shown below progress bar in a clean grid:
         - Win Rate: {winRate}% (large text)
         - Elo: {eloRating} ({eloTier}) -- use eloTier() from elo.ts
         - Games: {wins}W / {losses}L / {draws}D
         - Avg Turns: {avgTurns}
       - Matchup mini-table: opponent name + win rate, filling in progressively as games complete
       - Cancel button (destructive variant): "Cancel Simulation"
       - When status is 'complete' or 'cancelled': hide progress bar, show completion message
       - If cancelled: show "Cancelled after {gamesCompleted}/{gamesTotal} games"
       - Tailwind styling: subtle animations on stat updates (no heavy animations per user preferences)
  </action>
  <verify>
    <automated>cd C:/Users/ColeT/Code/java-2D/forge-mtg/forge-gui-web/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - SimulationConfig.tsx exports SimulationConfig component with game count selector [10, 50, 100, 500]
    - SimulationConfig has "Configure Gauntlet" expandable section with deck checkboxes
    - SimulationConfig has "Start Simulation" button calling onStart
    - SimulationProgress.tsx exports SimulationProgress component with progress bar
    - SimulationProgress shows running win rate, Elo with tier, W/L/D record
    - SimulationProgress has Cancel button calling onCancel
    - SimulationProgress handles 'complete' and 'cancelled' states
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Configuration panel allows selecting game count and opponent gauntlet. Progress display shows live-updating stats with progress bar and cancel button.</done>
</task>

<task type="auto">
  <name>Task 2: SimulationPanel container and DeckEditor integration</name>
  <files>
    forge-gui-web/frontend/src/components/simulation/SimulationPanel.tsx,
    forge-gui-web/frontend/src/components/deck-editor/DeckEditor.tsx,
    forge-gui-web/frontend/src/components/deck-editor/DeckPanel.tsx
  </files>
  <read_first>
    forge-gui-web/frontend/src/components/simulation/SimulationConfig.tsx,
    forge-gui-web/frontend/src/components/simulation/SimulationProgress.tsx,
    forge-gui-web/frontend/src/components/deck-editor/DeckEditor.tsx,
    forge-gui-web/frontend/src/components/deck-editor/DeckPanel.tsx,
    forge-gui-web/frontend/src/hooks/useSimulation.ts,
    forge-gui-web/frontend/src/lib/simulation-types.ts
  </read_first>
  <action>
    1. **SimulationPanel.tsx** -- Main simulation container (replaces DeckPanel content when active):
       - Props: { deckName: string, format: string, onClose: () => void }
       - Uses useSimulation(deckName) hook
       - State machine: 'config' -> 'running' -> 'results'
         - config: shows SimulationConfig
         - running: shows SimulationProgress
         - results: shows results tabs (placeholder div for now -- Plans 04/05 will add OverviewTab, MatchupsTab, etc.)
       - When startSim completes, transitions to 'running'
       - When progress.status becomes 'complete' or 'cancelled', transitions to 'results'
       - Header with "Deck Simulation" title and close (X) button
       - In 'results' state: show "Run Again" button to go back to 'config'
       - In 'results' state: results tabs placeholder -- render a div with id="simulation-results" and text "Results will appear here" (Plans 04/05 will replace this)
       - Pass progress data to SimulationProgress and (later) result tab components

    2. **DeckPanel.tsx** -- Add Simulate button:
       - Add onSimulate optional prop to DeckPanelProps
       - Add a Simulate button (FlaskConical or Beaker icon from lucide-react) in the action buttons row, next to Play button
       - Button calls onSimulate when clicked
       - Only show when onSimulate prop is provided

    3. **DeckEditor.tsx** -- Wire simulation panel:
       - Add state: const [showSimulation, setShowSimulation] = useState(false)
       - Pass onSimulate={() => setShowSimulation(true)} to DeckPanel
       - When showSimulation is true, render SimulationPanel instead of the card search panel (left column)
       - SimulationPanel onClose={() => setShowSimulation(false)} restores normal view
       - This way the deck panel stays visible on the right while simulation runs on the left
  </action>
  <verify>
    <automated>cd C:/Users/ColeT/Code/java-2D/forge-mtg/forge-gui-web/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - SimulationPanel.tsx exports SimulationPanel with config/running/results state machine
    - SimulationPanel uses useSimulation hook
    - SimulationPanel shows SimulationConfig in 'config' state, SimulationProgress in 'running' state
    - DeckPanel.tsx has onSimulate prop and Simulate button with icon
    - DeckEditor.tsx toggles between card search and SimulationPanel
    - SimulationPanel has close button to return to normal deck editor
    - Results state shows placeholder div with id="simulation-results"
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>User can click Simulate in deck editor, configure game count and gauntlet, start simulation, see live progress, cancel, and close simulation panel to return to deck editing.</done>
</task>

</tasks>

<verification>
- Simulate button appears in DeckPanel action row
- Clicking Simulate replaces card search with SimulationPanel
- SimulationConfig shows game count options and gauntlet configuration
- SimulationProgress shows progress bar and running stats
- Cancel button is visible during running state
- Close button returns to normal deck editor
- TypeScript compiles without errors
</verification>

<success_criteria>
Complete simulation launch flow: Simulate button -> configuration -> start -> live progress -> cancel/complete -> results placeholder. User can trigger simulation from deck builder (SIM-01), configure game count (SIM-02), and see real-time progress (SIM-12).
</success_criteria>

<output>
After completion, create `.planning/phases/12-deck-simulation/12-03-SUMMARY.md`
</output>
