---
phase: 12-deck-simulation
plan: 05
type: execute
wave: 4
depends_on: [12-04]
files_modified:
  - forge-gui-web/frontend/src/components/simulation/PerformanceTab.tsx
  - forge-gui-web/frontend/src/components/simulation/ManaTab.tsx
  - forge-gui-web/frontend/src/components/simulation/SimulationHistory.tsx
  - forge-gui-web/frontend/src/components/simulation/SimulationPanel.tsx
autonomous: true
requirements: [SIM-07, SIM-08, SIM-09]

must_haves:
  truths:
    - "User can see per-card win rate when drawn and dead card rate"
    - "User can see mana screw and flood rates"
    - "User can see resource stats (avg cards drawn, empty hand turns, life at win/loss)"
    - "User can view history of past simulation runs for the current deck"
  artifacts:
    - path: "forge-gui-web/frontend/src/components/simulation/PerformanceTab.tsx"
      provides: "Per-card performance table"
      contains: "cardPerformance"
    - path: "forge-gui-web/frontend/src/components/simulation/ManaTab.tsx"
      provides: "Mana screw/flood rates and resource stats"
      contains: "manaScrew"
    - path: "forge-gui-web/frontend/src/components/simulation/SimulationHistory.tsx"
      provides: "Past simulation results list"
      contains: "SimulationHistoryEntry"
  key_links:
    - from: "SimulationPanel.tsx"
      to: "PerformanceTab.tsx"
      via: "Renders in Performance tab"
      pattern: "PerformanceTab"
    - from: "SimulationPanel.tsx"
      to: "ManaTab.tsx"
      via: "Renders in Mana tab"
      pattern: "ManaTab"
---

<objective>
Build the Performance tab (per-card stats), Mana tab (screw/flood/resource stats), SimulationHistory component, and wire them into SimulationPanel's tab navigation.

Purpose: These tabs complete the simulation dashboard, giving users deep insights into individual card contributions and mana consistency.
Output: All 4 dashboard tabs functional with per-card and mana statistics. History component shows past simulation runs.
</objective>

<execution_context>
@C:/Users/ColeT/Code/java-2D/forge-mtg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ColeT/Code/java-2D/forge-mtg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-deck-simulation/12-CONTEXT.md
@.planning/phases/12-deck-simulation/12-02-SUMMARY.md

<interfaces>
From simulation-types.ts (Plan 02):
- CardPerformance { gamesDrawn, totalGames, winRateWhenDrawn, deadCardRate }
- SimulationProgress { cardPerformance: Record<string, CardPerformance>, manaScrew, manaFlood, avgCardsDrawn, avgEmptyHandTurns, avgLifeAtWin, avgLifeAtLoss, ... }
- SimulationHistoryEntry { id, timestamp, gamesCompleted, gamesTotal, winRate, eloRating }

From useSimulation.ts (Plan 02):
- history: SimulationHistoryEntry[] from TanStack Query
- refreshHistory: invalidates and refetches history

From elo.ts (Plan 02):
- eloTier(elo: number): string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PerformanceTab and ManaTab components</name>
  <files>
    forge-gui-web/frontend/src/components/simulation/PerformanceTab.tsx,
    forge-gui-web/frontend/src/components/simulation/ManaTab.tsx
  </files>
  <read_first>
    forge-gui-web/frontend/src/lib/simulation-types.ts,
    forge-gui-web/frontend/src/components/simulation/MatchupsTab.tsx,
    forge-gui-web/frontend/src/components/simulation/OverviewTab.tsx
  </read_first>
  <action>
    1. **PerformanceTab.tsx** -- Per-card performance table:
       - Props: { cardPerformance: Record<string, CardPerformance>, totalGames: number }
       - Sortable table with columns: Card Name, Games Drawn, Draw Rate, Win Rate When Drawn, Dead Card Rate
       - Draw Rate = gamesDrawn / totalGames (how often the card appears in a game)
       - Win Rate When Drawn: colored green > 60%, yellow 40-60%, red < 40%
       - Dead Card Rate: percentage of games where card was drawn but never cast (stuck in hand)
       - Dead cards highlighted with red text when > 30%
       - Default sort: by win rate when drawn descending (best performers first)
       - Clickable column headers to toggle sort (ascending/descending)
       - Summary stats at top:
         - "Best performer: {cardName} ({winRate}% win rate when drawn)"
         - "Worst performer: {cardName} ({winRate}%)"
         - "Most dead: {cardName} ({deadRate}% dead card rate)"
       - If no card data, show "No per-card data available"
       - Cards with very few draws (< 3 games) shown with dimmed/muted text ("insufficient data")

    2. **ManaTab.tsx** -- Mana and resource statistics:
       - Props: { data: SimulationProgress }
       - Two sections: "Mana Consistency" and "Resources"
       - **Mana Consistency section:**
         - Mana Screw Rate: {manaScrew * 100}% -- with visual bar (red fill)
         - Mana Flood Rate: {manaFlood * 100}% -- with visual bar (blue fill)
         - Interpretation text: if screw > 0.2, show "Consider adding more lands"; if flood > 0.15, show "Consider cutting some lands"
         - Visual health indicator: green check if both < 0.1, yellow warning if either 0.1-0.2, red alert if either > 0.2
       - **Resources section (stat cards in 2x2 grid):**
         - Avg Cards Drawn: {avgCardsDrawn} per game
         - Empty Hand Turns: {avgEmptyHandTurns} per game (red if > 2)
         - Avg Life at Win: {avgLifeAtWin} (how healthy are wins?)
         - Avg Life at Loss: {avgLifeAtLoss} (how close are losses?)
       - Color-code resource stats to provide quick readability
       - Consistent styling with other tabs (stat cards pattern from OverviewTab)
  </action>
  <verify>
    <automated>cd C:/Users/ColeT/Code/java-2D/forge-mtg/forge-gui-web/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - PerformanceTab.tsx renders sortable table with card name, draw rate, win rate when drawn, dead card rate
    - PerformanceTab has clickable column headers for sorting
    - PerformanceTab shows best/worst performer summary
    - PerformanceTab color-codes win rates (green/yellow/red)
    - ManaTab.tsx shows mana screw and flood rates with visual bars
    - ManaTab shows interpretation text for high screw/flood
    - ManaTab shows resource stats (cards drawn, empty hand, life at win/loss)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Performance tab shows per-card win rates, draw rates, and dead card detection with sortable columns. Mana tab shows screw/flood rates with visual indicators and resource statistics.</done>
</task>

<task type="auto">
  <name>Task 2: SimulationHistory component and final SimulationPanel wiring</name>
  <files>
    forge-gui-web/frontend/src/components/simulation/SimulationHistory.tsx,
    forge-gui-web/frontend/src/components/simulation/SimulationPanel.tsx
  </files>
  <read_first>
    forge-gui-web/frontend/src/components/simulation/SimulationPanel.tsx,
    forge-gui-web/frontend/src/components/simulation/PerformanceTab.tsx,
    forge-gui-web/frontend/src/components/simulation/ManaTab.tsx,
    forge-gui-web/frontend/src/hooks/useSimulation.ts,
    forge-gui-web/frontend/src/lib/simulation-types.ts,
    forge-gui-web/frontend/src/lib/elo.ts,
    forge-gui-web/frontend/src/api/simulation.ts
  </read_first>
  <action>
    1. **SimulationHistory.tsx** -- Past simulation results list:
       - Props: { history: SimulationHistoryEntry[], onSelect: (id: string) => void, onDelete: (id: string) => void }
       - List of past runs, each showing: date/time, games played, win rate, Elo rating
       - Each entry clickable to load that result (calls onSelect with id)
       - Delete button (trash icon) per entry (calls onDelete with id)
       - Format timestamp as relative ("2 hours ago") or absolute ("Mar 21, 10:30 AM")
       - Elo shown with tier label from eloTier()
       - Win rate shown with color coding (green/yellow/red)
       - Empty state: "No simulation history for this deck"
       - Sort by timestamp descending (most recent first)

    2. **SimulationPanel.tsx** -- Final wiring:
       - Import PerformanceTab and ManaTab
       - Replace Performance tab placeholder with: <PerformanceTab cardPerformance={progress.cardPerformance} totalGames={progress.gamesCompleted} />
       - Replace Mana tab placeholder with: <ManaTab data={progress} />
       - Add "History" section below the tabs (or as a 5th tab):
         - Render SimulationHistory with history from useSimulation hook
         - onSelect: load that result via getSimulationStatus(id) and display it in the tabs
         - onDelete: call deleteSimulationResult(id) and refreshHistory
       - When loading a historical result, show it in the same tabs interface
       - Add state for selectedHistoryId to differentiate between current run and historical view
       - When viewing history, show "Viewing saved result from {timestamp}" banner and "Back to latest" button
  </action>
  <verify>
    <automated>cd C:/Users/ColeT/Code/java-2D/forge-mtg/forge-gui-web/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - SimulationHistory.tsx renders list of past runs with date, games, win rate, Elo
    - SimulationHistory has clickable entries and delete buttons
    - SimulationPanel.tsx Performance tab renders PerformanceTab component (not placeholder)
    - SimulationPanel.tsx Mana tab renders ManaTab component (not placeholder)
    - SimulationPanel has History section with SimulationHistory
    - Historical result selection loads and displays saved results in tabs
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All 4 dashboard tabs (Overview, Matchups, Performance, Mana) render real components. SimulationHistory shows past runs with load and delete. User can view current or historical simulation results.</done>
</task>

</tasks>

<verification>
- PerformanceTab renders per-card table with sorting
- ManaTab shows screw/flood rates and resource stats
- SimulationHistory lists past runs
- All 4 tabs in SimulationPanel render actual components
- Historical results can be loaded and viewed
- TypeScript compiles without errors
</verification>

<success_criteria>
Performance tab shows per-card win rate and dead card rate (SIM-09). Mana tab shows screw/flood rates and resource stats (SIM-07, SIM-08). History component enables reviewing past simulation results. All dashboard tabs complete.
</success_criteria>

<output>
After completion, create `.planning/phases/12-deck-simulation/12-05-SUMMARY.md`
</output>
