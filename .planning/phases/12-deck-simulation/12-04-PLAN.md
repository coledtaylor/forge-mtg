---
phase: 12-deck-simulation
plan: 04
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - forge-gui-web/frontend/src/components/simulation/OverviewTab.tsx
  - forge-gui-web/frontend/src/components/simulation/MatchupsTab.tsx
  - forge-gui-web/frontend/src/components/simulation/PlaystyleRadar.tsx
  - forge-gui-web/frontend/src/components/simulation/SimulationPanel.tsx
autonomous: true
requirements: [SIM-04, SIM-05, SIM-06, SIM-10, SIM-11]

must_haves:
  truths:
    - "User can see overall win rate prominently displayed after simulation"
    - "User can see Elo rating with tier label (Weak/Average/Strong/Elite)"
    - "User can see playstyle radar chart with 4 axes (Aggro/Midrange/Control/Combo)"
    - "User can see per-matchup win rate table with opponent names and records"
    - "User can see win rate on play vs draw"
    - "User can see mulligan stats (keep rate, average mulligans)"
    - "User can see speed stats (avg kill turn, fastest/slowest win)"
  artifacts:
    - path: "forge-gui-web/frontend/src/components/simulation/OverviewTab.tsx"
      provides: "Elo, playstyle radar, win rate, headline stats"
      contains: "eloRating"
    - path: "forge-gui-web/frontend/src/components/simulation/MatchupsTab.tsx"
      provides: "Per-opponent breakdown table"
      contains: "matchups"
    - path: "forge-gui-web/frontend/src/components/simulation/PlaystyleRadar.tsx"
      provides: "SVG radar chart for playstyle classification"
      contains: "polygon"
  key_links:
    - from: "OverviewTab.tsx"
      to: "PlaystyleRadar.tsx"
      via: "Renders radar chart with playstyle scores"
      pattern: "PlaystyleRadar"
    - from: "SimulationPanel.tsx"
      to: "OverviewTab.tsx"
      via: "Renders in results tabs"
      pattern: "OverviewTab"
---

<objective>
Build the Overview and Matchups tabs of the simulation results dashboard, including the Elo display, playstyle radar chart, headline statistics, and per-opponent breakdown table.

Purpose: These tabs give the user the most important answers: "How good is my deck?" (Elo, win rate) and "What does it beat/lose to?" (matchups).
Output: Two result tabs with comprehensive win/loss data and a playstyle visualization.
</objective>

<execution_context>
@C:/Users/ColeT/Code/java-2D/forge-mtg/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ColeT/Code/java-2D/forge-mtg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-deck-simulation/12-CONTEXT.md
@.planning/phases/12-deck-simulation/12-02-SUMMARY.md

<interfaces>
From simulation-types.ts (Plan 02):
- SimulationProgress { winRate, winRateOnPlay, winRateOnDraw, eloRating, playstyle, matchups, wins, losses, draws, avgTurns, fastestWin, slowestWin, avgFirstThreatTurn, keepRate, avgMulligans, winRateAfterMulligan, gamesCompleted, gamesTotal, cancelled }
- MatchupStats { games, wins, winRate }

From elo.ts (Plan 02):
- eloTier(elo: number): string

From existing chart patterns:
- ManaCurveChart.tsx, ColorDistribution.tsx, TypeBreakdown.tsx -- SVG chart components
- All use Tailwind for sizing, pure SVG for rendering, no chart library
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlaystyleRadar SVG component and OverviewTab</name>
  <files>
    forge-gui-web/frontend/src/components/simulation/PlaystyleRadar.tsx,
    forge-gui-web/frontend/src/components/simulation/OverviewTab.tsx
  </files>
  <read_first>
    forge-gui-web/frontend/src/lib/simulation-types.ts,
    forge-gui-web/frontend/src/lib/elo.ts,
    forge-gui-web/frontend/src/components/charts/ManaCurveChart.tsx,
    forge-gui-web/frontend/src/components/charts/ColorDistribution.tsx
  </read_first>
  <action>
    1. **PlaystyleRadar.tsx** -- SVG radar chart with 4 axes:
       - Props: { scores: Record<string, number> } where keys are 'aggro', 'midrange', 'control', 'combo' and values 0.0-1.0
       - SVG viewBox="0 0 200 200", center at (100, 100), radius 80
       - 4 axes at 90-degree intervals (top=aggro, right=midrange, bottom=control, left=combo)
       - Draw grid rings at 25%, 50%, 75%, 100% as polygons with subtle stroke
       - Draw axis lines from center to edge
       - Draw data polygon with fill using primary color at 30% opacity and stroke at full primary
       - Axis labels positioned outside the chart (aggro top, midrange right, control bottom, combo left)
       - Show percentage value on each axis label: "Aggro 72%"
       - Responsive sizing via className prop (default w-48 h-48)
       - Use currentColor for grid lines, hsl(var(--primary)) for data polygon
       - No chart library -- pure SVG like existing charts

    2. **OverviewTab.tsx** -- Main results overview:
       - Props: { data: SimulationProgress }
       - Layout: two-column grid on large screens, single column on small
       - Left column: Elo display + Playstyle radar
         - Elo: large number (e.g., "1623") with tier label below (e.g., "Above Average") using eloTier()
         - Elo number colored: red <1400, yellow 1400-1549, green 1550+
         - PlaystyleRadar component below with playstyle scores
       - Right column: stat cards grid (2x3 or 3x2)
         - Win Rate: {winRate}% with W/L/D record ({wins}-{losses}-{draws})
         - On Play: {winRateOnPlay}% win rate
         - On Draw: {winRateOnDraw}% win rate
         - Avg Kill Turn: {avgTurns} turns (fastest: {fastestWin}, slowest: {slowestWin})
         - Mulligans: {keepRate}% keep rate, avg {avgMulligans} mulligans
         - First Threat: Turn {avgFirstThreatTurn} average
       - If data.cancelled: show banner "Results from {gamesCompleted}/{gamesTotal} games (cancelled early)"
       - Stat cards use subtle borders, consistent padding, muted label text with bold value
       - Games played summary: "{gamesCompleted} games against {Object.keys(matchups).length} opponents"
  </action>
  <verify>
    <automated>cd C:/Users/ColeT/Code/java-2D/forge-mtg/forge-gui-web/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - PlaystyleRadar.tsx renders SVG radar chart with 4 labeled axes
    - PlaystyleRadar shows grid rings and data polygon
    - OverviewTab.tsx shows Elo rating with tier from eloTier()
    - OverviewTab shows win rate, on-play/on-draw rates, avg turns, mulligan stats
    - OverviewTab shows PlaystyleRadar with playstyle data
    - OverviewTab handles cancelled state with banner
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Overview tab displays Elo rating with tier, playstyle radar chart, and 6 headline stat cards (win rate, play/draw, speed, mulligans, first threat).</done>
</task>

<task type="auto">
  <name>Task 2: MatchupsTab and SimulationPanel tab wiring</name>
  <files>
    forge-gui-web/frontend/src/components/simulation/MatchupsTab.tsx,
    forge-gui-web/frontend/src/components/simulation/SimulationPanel.tsx
  </files>
  <read_first>
    forge-gui-web/frontend/src/components/simulation/OverviewTab.tsx,
    forge-gui-web/frontend/src/components/simulation/SimulationPanel.tsx,
    forge-gui-web/frontend/src/lib/simulation-types.ts,
    forge-gui-web/frontend/src/components/ui/tabs.tsx
  </read_first>
  <action>
    1. **MatchupsTab.tsx** -- Per-opponent breakdown table:
       - Props: { matchups: Record<string, MatchupStats>, winRateOnPlay: number, winRateOnDraw: number }
       - Table with columns: Opponent, Games, Wins, Losses, Win Rate, Bar
       - Each row: opponent deck name, games count, wins count, (games-wins) losses, win rate percentage
       - Win rate bar: inline horizontal bar (green fill proportional to win rate, red for remainder)
       - Color-code win rates: green > 60%, yellow 40-60%, red < 40%
       - Sort by win rate descending (best matchups first)
       - Total row at bottom with overall stats
       - Summary section below table:
         - "Best matchup: {opponent} ({winRate}%)"
         - "Worst matchup: {opponent} ({winRate}%)"
         - "Play vs Draw: {winRateOnPlay}% on play, {winRateOnDraw}% on draw"
       - If no matchups yet, show "No matchup data available"

    2. **SimulationPanel.tsx** -- Update results state to render tabs:
       - Replace the placeholder div (id="simulation-results") with proper tabbed interface
       - Import OverviewTab and MatchupsTab
       - Use shadcn Tabs component with TabsList/TabsTrigger/TabsContent
       - Tabs: "Overview", "Matchups", "Performance", "Mana"
       - Overview tab: render OverviewTab with progress data
       - Matchups tab: render MatchupsTab with matchups data
       - Performance tab: placeholder text "Per-card performance stats" (Plan 05)
       - Mana tab: placeholder text "Mana statistics" (Plan 05)
       - Default to Overview tab
       - "Run Again" button above tabs to restart
       - Results tabs should only show when in 'results' state (progress.status is 'complete' or 'cancelled')
  </action>
  <verify>
    <automated>cd C:/Users/ColeT/Code/java-2D/forge-mtg/forge-gui-web/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - MatchupsTab.tsx renders table with opponent name, games, wins, losses, win rate columns
    - MatchupsTab has inline win rate bars with color coding (green/yellow/red)
    - MatchupsTab shows best/worst matchup summary
    - SimulationPanel.tsx has 4-tab layout (Overview, Matchups, Performance, Mana)
    - Overview and Matchups tabs render real components
    - Performance and Mana tabs show placeholder text
    - "Run Again" button visible in results state
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Matchups tab shows per-opponent win rates with visual bars. SimulationPanel wires Overview and Matchups tabs. User can see overall win rate, Elo, playstyle classification, per-matchup breakdown, and play/draw splits.</done>
</task>

</tasks>

<verification>
- PlaystyleRadar renders SVG with 4 axes and data polygon
- OverviewTab shows Elo with tier, win rate, speed stats, mulligan stats
- MatchupsTab shows per-opponent table with win rate bars
- SimulationPanel has working tab navigation between Overview and Matchups
- All TypeScript compiles
</verification>

<success_criteria>
Overview tab displays Elo rating, playstyle radar, and headline stats (SIM-04, SIM-05, SIM-06, SIM-10, SIM-11). Matchups tab shows per-opponent win rates (SIM-04). Tab navigation works in SimulationPanel.
</success_criteria>

<output>
After completion, create `.planning/phases/12-deck-simulation/12-04-SUMMARY.md`
</output>
